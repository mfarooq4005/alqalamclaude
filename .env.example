# Copy to .env in repo root (same folder as alqalam_node_server.js)

PORT=3000
APP_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=alqalam_db
DB_USERNAME=root
DB_PASSWORD=

JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRY=24h

# Optional
ATTENDANCE_LATE_TIME=08:30
